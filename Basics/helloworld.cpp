#include <iostream> //Here we are importing Input and out put stream so as to use it in the code
using namespace std; // Removes repeated usage of std when we are coding anything
int main(){
    //Compiler is like "yahi chalyaenge, warna nahi chalyaenge" 
    //All the codes inside this function is run at the time of execution.
    int i;
    cin>>i;
    //cin : Takes imput from the user.
    cout<<"Output is"<<i;
    //cout : Returns a output of the required statement to the user
    //Prints something to the console
    
    return 0;
    // As Main is an integer function hence it always returns a integer.
    //so it has to return an integer 
}